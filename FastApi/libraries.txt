=== img to pdf =====

pip install fastapi[all]
pip install Pillow img2pdf uvicorn

=== pdf to doc =====
pip install python-docx reportlab

# image to pdf 
pip install fpdf
pip install docx2pdf
pip install PyPDF2
pip install pdf2docx


# remove image background 

# remove background 
pip install rembg
pip install shutil


# replace background 


# Extract text from image
pip install easyocr




uvicorn main:app --host 0.0.0.0 --port 8000 --reload
